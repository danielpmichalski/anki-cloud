# Anki sync server URL — shown in the web UI so users know where to point Anki Desktop
# Must match the host/port the sync server is reachable on from the user's browser
VITE_SYNC_SERVER_URL=http://localhost:8080
